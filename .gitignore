# Python
__pycache__/
*.py[cod]
*.so
.pytest_cache/
.venv/
venv/

# Environment
.env

# OS / editor
.DS_Store
.vscode/
.idea/

# Apex runtime artifacts
.apex/*
# The rewrite rule book is durable project config, not a runtime artifact:
# it must travel with the repo so the nightly `apex rewrite --all` enforces it.
!.apex/rewrite-rules.json
.epistemic/
*.log

# Test / coverage artifacts
.coverage
.coverage.*
htmlcov/

# Claude Code agent worktrees (ephemeral)
.claude/worktrees/

# build artifacts
dist/
build/
*.egg-info/
